{% extends 'base.html.twig' %}

{% block title %}Planning{% endblock %}

{% block body %}
    <h1>Planning</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planning.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ planning.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ planning.contenu }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ planning.dateDebut }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ planning.dateFin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('planning_index') }}">back to list</a>

    <a href="{{ path('planning_edit', {'id': planning.id}) }}">edit</a>

    {{ include('planning/_delete_form.html.twig') }}
{% endblock %}